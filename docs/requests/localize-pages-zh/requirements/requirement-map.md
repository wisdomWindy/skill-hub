# 拆分摘要

本请求拆分为一个交付模块：`module-01-localize-visible-copy`。

# 来源模块

- 用户直接请求：把当前项目中所有页面的文本改成中文。

# 拆分模块与功能单元

## module-01-localize-visible-copy

- 首页文案中文化。
- 技能列表页文案中文化。
- 技能详情页文案中文化。
- 公共组件文案中文化。
- 示例技能展示数据中文化。
- 测试期望值同步修正。

# 来源追踪

- 用户请求全文即为来源。
- 代码扫描发现页面文案集中在 `src/views`、`src/components/common`、`src/features/skills/components` 和 `_data`。

# 模块依赖

- 依赖现有 Vue/Vite/TypeScript 项目结构。
- 不依赖后端接口。

# 执行顺序

1. 补齐 TypeScript 与数据上下文。
2. 修改页面、组件和数据文案。
3. 同步测试期望值。
4. 执行验证。

# 页面设计路由提示

- 不需要 page-design。需求只改文本，不改变布局、视觉层级或交互结构。

# 架构设计路由提示

- 不需要 architecture-design。不新增模块边界、数据结构或抽象层。

# Spec 输入检查清单

- 保留机器字段和命令。
- 用户可见静态文案全部中文化。
- 乱码中文全部修正。
- 搜索、筛选、排序、分页、复制命令行为不变。

# 开放问题

- 无。
