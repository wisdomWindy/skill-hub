# 计划模板

## 必需工件路径

- 非拆分工作：`docs/requests/<request-id>/plan/plan.md`、`plan/task-board.md`
- 拆分 PRD 工作：`docs/requests/<request-id>/module-runs/<module-id>/plan/plan.md`、`plan/task-board.md`

## `plan/plan.md` 或 `module-runs/<module-id>/plan/plan.md`

必需章节：

- 交付单元标识
- 阅读导航
- 全局摘要
- 用户意图合同承接（如适用）
- 任务拆解
- 功能拆解明细
- 项目脚手架与初始化策略（greenfield 适用）
- API 对接与类型策略
- 依赖关系
- 变更链路审查（修改或移除既有代码适用）
- 整洁性与复杂度控制
- 函数式编程约束（如适用）
- Tailwind 样式约束（如适用）
- 模式决策与替代方案
- 代码上下文与影响范围
- 并行执行建议（含是否值得启用 workflow）
- 触发与上下文准备
- 受影响文件或模块
- 测试策略
- 观察与人工介入点
- 回滚说明

排版要求：

- 使用清晰标题层级，优先让读者在 10 到 20 秒内定位到任一任务信息
- 优先使用短段落、列表、表格、任务卡片式分节
- 避免整页连续大段散文
- 每个任务必须独立成节，且标题中带任务 ID 与任务名称
- 每个任务节内必须包含流程图，默认使用 Mermaid

### `阅读导航`

建议内容：

- 请求目标摘要
- 任务总数
- 串行任务数 / 可并行任务数
- 高风险任务
- 关键依赖
- 文档内跳转索引

### `全局摘要`

至少写清：

- 本次计划覆盖的业务范围
- 计划执行主线
- 最关键的状态流转或交互主线
- 最大风险点
- 实施前必须满足的前置条件

### `用户意图合同承接`

当 spec 包含 user intent contract 时，至少写清：

- 用户字面要求
- 实际目标
- 成功标准
- 禁止解释 / 禁止规避方式
- 哪些任务负责落实 intent compliance
- 哪些验证项负责证明没有把复杂度、风险、歧义或责任转移到别处

### `任务拆解`

每个任务至少写清：

- 任务 ID 与任务名称
- 任务目标
- 关联规格区域
- 受影响页面、模块、弹窗、抽屉、卡片、区块或流程节点
- 串行/并行属性
- 前置条件与完成条件
- 对 `execute` 的禁止猜测边界
- 对 `execute` 的用户意图与禁止解释约束（如适用）
- 对 `execute` 的变更前链路审查和变更后链路复查要求（修改或移除既有代码时适用）
- 任务流程图

推荐任务小节结构：

- 任务目标
- 规格映射
- 范围与影响面
- 前置条件
- 实现子项
- 交互与状态约束
- API 与数据约束
- 函数式边界与副作用边界
- 测试与验证要点
- 风险与回退
- Mermaid 流程图

### `任务流程图`

硬要求：

- 每个任务必须提供一个 Mermaid 流程图
- 图中至少包含开始节点、关键处理步骤、判断分支、成功出口、失败或回退出口
- 如果任务覆盖表单、表格、异步加载、审核流、状态流转等内容，流程图必须体现这些关键状态变化
- 如果任务是多个子步骤串联，流程图不能只画成单节点占位图

### `功能拆解明细`

对于任务涉及的每一个功能单元，必须按实际形态继续细拆，直到 `execute` 可直接照此实现而不需要补猜需求。

如果是表单，至少写清：

- 表单所在页面或容器
- 表单用途
- 字段清单
- 字段顺序
- 每个字段的字段名
- 每个字段的业务含义
- 每个字段是必填还是选填
- 每个字段对应控件类型
- 默认值或回填规则
- 占位提示或帮助信息（如有）
- 可编辑/禁用条件
- 显隐条件
- 选项来源或联动关系
- 校验规则
- 输入值归一策略（如保留原值、`trim`、其他规范化处理）
- 纯空格输入如何处理
- 归一化后为空如何处理
- 长度校验按原值还是归一化后值计算
- 前后空白、连续空白、换行、粘贴内容、非法字符的处理口径
- 校验触发时机与提示时机
- 提交按钮与触发方式
- 提交后的成功反馈、失败反馈与 loading 结束条件

如果是表格，至少写清：

- 表格所在页面或容器
- 表格用途
- 列顺序
- 每列列名
- 每列字段来源
- 每列展示格式
- 空值展示规则
- 行内操作
- 批量操作
- 排序/筛选/分页/滚动规则（如有）
- 空状态、异常状态、loading 状态及取消条件

如果是纯展示内容，至少写清：

- 展示所在页面或容器
- 展示字段清单
- 字段分组或布局方式
- 每个字段展示格式
- 空值展示规则
- 显隐条件
- 是否只读、可折叠、可跳转或可复制

如果是交互行为，至少写清：

- 触发入口
- 触发前置条件
- 点击或操作后的界面变化
- 调用的动作或副作用
- 是否需要二次确认
- 是否需要 loading
- loading 从何时开始
- loading 在什么成功/失败/中断条件下结束
- 成功提示、失败提示、重试方式

如果是流程型能力，至少写清：

- 起点与终点
- 关键步骤
- 分支条件
- 状态流转
- 异常分支
- 回退/重试规则
- 与其他页面、模块或服务的交接点

补充要求：

- 任何会影响实现正确性的未知项，必须在计划中写成“待确认项”或“已批准假设”，禁止留白。
- 任何字段、列、交互、状态、流程如果已经在 PRD/spec 中可确定，就必须落进计划，不得省略为“按常规实现”。
- 对文本输入类控件，凡实现阶段可能自行脑补的隐式输入语义，都必须在计划中落明，尤其是纯空格、归一化空值、长度计算口径、空白字符处理、非法字符处理、校验与提示触发时机。
- 如任务修改或移除既有代码，必须写明操作前需要审查的完整功能链路、文件引用关系、调用方、副作用、下游消费者和邻近影响面；同时写明操作后如何复查链路正常、无缺少必要环节、无残留多余环节。
- 如任务移除调用、请求、分支、字段、控件或副作用，必须写明删除依赖闭包清理项：import、helper、constant、type、request wrapper、state、computed、watch、test、mock、comment；保留 helper 时必须写明真实生产调用方。仅测试文件引用待改或待删代码时视为空引用，必须把测试列为适配项。
- 如任务涉及业务规则、校验、筛选排序、payload 构造、状态派生、adapter / mapper / `fromDetail` 转换，必须写明纯函数边界、不可变输入、副作用边界、语义归一层级和验证方式。
- 如涉及新增或修改样式，必须写明只允许使用 Tailwind CSS-style utility classes；`class` / `className` / class binding 值不得超过项目 formatter 正常行宽或依赖多行包裹；不得用常量、map、computed、helper 或 import 变量隐藏过长 class 值；过长时必须拆分结构、提取更小组件或降低样式复杂度。
- 如果 `requirements/modules/` 已经拆出模块级内容，任务拆解必须显式覆盖每个在范围内的模块，不能跳过任何已拆出的功能单元。
- 任务拆解、功能拆解明细与流程图三者必须互相一致，不能出现图文不一致或图缺步骤、文缺分支的情况。

### `项目脚手架与初始化策略`

当 scoped work 为从 0 开始搭建项目、应用、包或前端业务面时，至少写清：

- 是否存在合适的同类型项目脚手架或 starter
- 计划采用的脚手架来源
- 为什么它适合当前项目类型
- 若不采用脚手架，拒绝原因是什么
- 脚手架初始化之后需要继续裁剪、替换或补齐的改造项
- 哪些 bootstrap 决策在执行阶段不得重新发明

### `API 对接与类型策略`

如果需求涉及服务端接口对接，至少写清：

- 接口契约来源
  - 服务端 TypeScript 声明
  - protobuf
  - 常规接口文档
  - 其他正式契约来源
- 每个接口的用途
- 请求层改动范围
- 类型复用或类型转换方案
- 是否需要 adapter / mapper
- 如果服务端已有 TypeScript 类型声明，前端将直接复用哪些类型
- 如果服务端没有 TypeScript 类型声明，将如何在保持字段名与服务端一致的前提下写出 TypeScript 类型
- protobuf 类型是直接复用生成类型还是从 protobuf 契约导出前端可用类型
- 哪些字段语义需要在适配层归一
- 哪些错误态、空态、loading 态依赖接口结果驱动
- 联调、mock、回归验证分别覆盖哪些接口面

### `函数式编程约束`

当任务涉及业务规则、校验、筛选排序、分组、payload 构造、状态派生、详情到表单回填、接口数据到视图模型转换、adapter / mapper 归一时，至少写清：

- 哪些逻辑必须是纯函数或纯 mapper
- 哪些输入必须按不可变数据处理
- 哪些副作用只能出现在 request layer、event handler、store action、command function 或 lifecycle boundary
- 哪些数据语义归一必须在 adapter / mapper / `fromDetail` 完成
- 哪些 derived state 不应重复保存成 writable state
- 哪些测试或验证覆盖纯函数、转换链路和副作用边界
- 是否禁止引入函数式工具库、过度 currying、point-free 或长 reducer chain

### `变更链路审查`

当任务修改或移除既有代码时，至少写清：

- 变更目标属于哪条功能链路
- 用户入口或内部触发入口
- 涉及的页面、组件、hook、store、service、adapter、helper、request wrapper
- 涉及的 import / export、调用方、消费者、事件、computed、watch、测试、mock
- 涉及的状态流转、副作用、请求、下游 UI 或数据消费者
- 哪些环节由本次变更 owning，哪些只是邻近影响面
- 哪些测试文件引用了待改或待删代码，以及这些测试如何更新、替换或删除
- 测试引用不能作为生产代码保留依据；真实调用方必须来自生产链路
- 修改或移除前必须确认的链路证据
- 修改或移除后必须复查的链路证据
- 判定链路干净的标准：不缺少必要环节、不保留多余环节、无重复路径、无冲突路径、无孤立符号、无未授权邻近功能影响

## `plan/task-board.md` 或 `module-runs/<module-id>/plan/task-board.md`

每个任务必需字段：

- 任务 ID
- 任务名称
- 状态
- 执行模式（串行 / 可并行）
- 执行人或执行说明
- 触发/前置条件
- 关联规格区域
- 覆盖功能单元
- 页面/模块/容器范围
- 整洁性约束或注意点
- 函数式编程约束或副作用边界（如适用）
- 变更链路审查要求（修改或移除既有代码时适用）
- 测试适配要求（测试引用待改或待删代码时适用）
- 模式约束或抽象边界
- 代码上下文或影响面备注
- 关键交互与状态约束
- API 契约来源与类型策略
- 测试切入点
- 待确认项或已批准假设
